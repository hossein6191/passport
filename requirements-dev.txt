# The offline suite: `pip install -r requirements-dev.txt` then `pytest tests/ -q` — no network, no Studio.
pytest==9.1.1
# The linter the deployed bytes are checked with (`genvm-lint check contracts/passport.py`):
genvm-lint==0.11.0
